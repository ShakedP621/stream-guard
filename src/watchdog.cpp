#include "streamguard/watchdog.hpp"
// Intentionally empty: all methods are inline in the header for now.
// This TU exists so we can later move implementations out of the header
// without changing the library surface.
